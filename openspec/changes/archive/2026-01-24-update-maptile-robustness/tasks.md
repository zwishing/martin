# Tasks: 强化 Maptile 并发与输入安全

## 1. 并发与生命周期
- [x] 1.1 在服务启动时创建可取消信号，并传入配置热重载任务
- [x] 1.2 热重载任务监听取消信号后退出
- [x] 1.3 调整 `get_tile` 获取 source 的方式，避免持锁跨 `await`

## 2. 输入与配置校验
- [x] 2.1 增加 z/x/y 合法性校验并返回明确 `TileError`
- [x] 2.2 增加 schema/table/column 标识符校验，非法配置跳过并记录 warn

## 3. 测试与验证
- [x] 3.1 增加坐标校验的单元测试
- [x] 3.2 增加标识符校验与跳过行为的单元测试
- [x] 3.3 运行 `cargo test -p maptile`
